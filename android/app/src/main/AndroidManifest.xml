<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Required prerequisite for the connectedDevice FGS type on Android 14+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Fallback FGS type if BLUETOOTH_CONNECT is not (yet) granted -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Doze-proof heartbeat: coroutine timers are deferred by HyperOS
         even in a foreground service (field 2026-08-29). USE_EXACT_ALARM
         auto-grants (sideload; alarm is life-safety-critical),
         SCHEDULE_EXACT_ALARM is the user-grantable fallback. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- SEND_SMS / CALL_PHONE removed 2026-08-29: they were the Play
         Protect "harmful app" trigger on a sideloaded unknown-cert APK,
         and nothing needs them today — the wearer's emergency dial uses
         ACTION_DIAL (permissionless) and Escalator's SMS path already
         no-ops without the grant. Restore in a dedicated gateway flavor
         when the SMS fallback ships for real (M4). -->

    <application
        android:name=".CmApp"
        android:icon="@mipmap/ic_launcher"
        android:label="Standby"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.CryoMonitor">

        <service
            android:name=".MonitorService"
            android:foregroundServiceType="connectedDevice|specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Life-safety monitor: maintains the Bluetooth watch link and escalates unresponsiveness alarms" />
        </service>

        <activity
            android:name=".DebugActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AlarmActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance" />

        <activity
            android:name=".LogActivity"
            android:exported="false" />

        <!-- Log / lab-result sharing as files (text extras crash on big logs) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".EnrollActivity"
            android:exported="false" />

        <activity
            android:name=".ContactsActivity"
            android:exported="false" />

        <!-- PebbleKit2: the Core app binds this when our watchapp opens -->
        <service
            android:name=".PebbleKit2ListenerService"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="io.rebble.pebblekit2.RECEIVE_DATA_FROM_WATCH" />
            </intent-filter>
        </service>

        <!-- Reboot/update recovery: monitoring must not stay dead until
             the wearer notices. Protected system broadcasts — only the
             system can send them. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Worker DataLogging heartbeats (M0 S5): legacy PebbleKit
             broadcast protocol; PebbleKit2 has no DataLogging API. -->
        <receiver
            android:name=".DataLogReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.getpebble.action.dl.RECEIVE_DATA" />
                <action android:name="com.getpebble.action.dl.FINISH_SESSION" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
